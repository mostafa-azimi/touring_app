# pnpm configuration for Vercel deployment
# Enable build scripts for essential packages
enable-pre-post-scripts=true

# Explicitly allow build scripts for packages that need them
# This resolves the Vercel deployment warning about ignored build scripts
@tailwindcss/oxide:build-scripts=true
esbuild:build-scripts=true
sharp:build-scripts=true
